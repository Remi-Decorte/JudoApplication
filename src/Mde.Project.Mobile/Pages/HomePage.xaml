<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="Mde.Project.Mobile.Pages.HomePage"
             Title="Home">

    <ScrollView>
        <VerticalStackLayout Padding="0" Spacing="0" BackgroundColor="LightGray">

            <!-- Top Bar -->
            <Grid BackgroundColor="Black" Padding="10" RowDefinitions="Auto" ColumnDefinitions="*,Auto">
                <!-- Left links -->
                <HorizontalStackLayout Spacing="20">
                    <Label Text="Live" TextColor="White"/>
                    <Label Text="Schedule" TextColor="White"/>
                    <Label Text="News" TextColor="White"/>
                </HorizontalStackLayout>

                <!-- Right links -->
                <HorizontalStackLayout Spacing="20" Grid.Column="1">
                    <Label Text="🔍 Search" TextColor="White"/>
                    <Label Text="👤 Login" TextColor="White"/>
                    <Frame BackgroundColor="#FBBF24" Padding="10,5" CornerRadius="8" HasShadow="False">
                        <Label Text="Subscribe" TextColor="Black" FontAttributes="Bold"/>
                    </Frame>
                </HorizontalStackLayout>
            </Grid>

        </VerticalStackLayout>
    </ScrollView>
</ContentPage>