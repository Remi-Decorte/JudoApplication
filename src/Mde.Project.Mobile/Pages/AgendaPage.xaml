<?xml version="1.0" encoding="utf-8" ?>
<ContentPage
    x:Class="Mde.Project.Mobile.Pages.AgendaPage"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    Title="Mijn Agenda">

    <Grid RowDefinitions="Auto,*" Padding="16" BackgroundColor="LightGray">

        <!-- Header -->
        <HorizontalStackLayout Spacing="12">
            <Label Text="Trainingen"
                   FontSize="24"
                   FontAttributes="Bold"
                   VerticalOptions="Center" />

            <Button Text="+ Training"
                    SemanticProperties.Description="Nieuwe training toevoegen"
                    HorizontalOptions="EndAndExpand"
                    Clicked="OnAddTrainingClicked"
                    BackgroundColor="#FBBF24"
                    TextColor="Black"
                    FontAttributes="Bold" />
        </HorizontalStackLayout>

        <!-- Body -->
        <Grid Grid.Row="1">
            <CollectionView ItemsSource="{Binding Trainings}">
                <CollectionView.EmptyView>
                    <VerticalStackLayout Padding="24" Spacing="8" HorizontalOptions="Center" VerticalOptions="Center">
                        <Label Text="Nog geen trainingen"
                               FontAttributes="Bold"
                               HorizontalTextAlignment="Center"/>
                        <Label Text="Klik op “+ Training” om je eerste item toe te voegen."
                               FontSize="12"
                               TextColor="Gray"
                               HorizontalTextAlignment="Center"/>
                    </VerticalStackLayout>
                </CollectionView.EmptyView>

                <CollectionView.ItemTemplate>
                    <DataTemplate>
                        <Frame Margin="6" Padding="12" CornerRadius="12" HasShadow="True" BackgroundColor="White">
                            <Grid ColumnDefinitions="Auto,*" RowDefinitions="Auto,Auto">
                                <Label Text="{Binding Date, StringFormat='{}{0:dd/MM/yyyy}'}"
                                       FontAttributes="Bold" />
                                <Label Grid.Row="1" Text="{Binding Type}" TextColor="Gray" />
                                <Label Grid.Column="1"
                                       Grid.RowSpan="2"
                                       HorizontalTextAlignment="End"
                                       Text="{Binding TechniqueScores.Count, StringFormat='Technieken: {0}'}"/>
                            </Grid>
                        </Frame>
                    </DataTemplate>
                </CollectionView.ItemTemplate>
            </CollectionView>
        </Grid>
    </Grid>
</ContentPage>

</ContentPage>

